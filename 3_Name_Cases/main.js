// Q: Name Cases: Store a person’s name in a variable, and then print that person’s name in lowercase, uppercase, and titlecase.
var person_name = "sYed huzaifa";
// For Upper Case:
console.log(person_name.toUpperCase());
// For Lower Case:
console.log(person_name.toLowerCase());
// For Title Case:
console.log(person_name[0].toUpperCase() + person_name.slice(1).toLowerCase());
